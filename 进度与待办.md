# Fairy 桌宠 — 进度与待办

更新时间：2026-09-12 23:40

---

## 一、已完成：实机动画素材分析（本轮核心成果）

### 素材

`ref_video/` 下 4 段 B 站实机录像：

| 文件 | 内容 | 可用性 |
|---|---|---|
| `egg.mp4`（BV131421k7xW） | H.D.D. 挂机彩蛋/待机合集，143s，Fairy 眼睛全程固定在画面左下 | ✅ 已完成逐帧分析 |
| `voice.mp4`（BV1KQa3eSE4V） | Fairy 待机语音整合，72s | ❌ 画面中检测不到眼睛（非 H.D.D. 界面特写） |
| `boot.f30080.mp4`（BV1jw4m1Y7MB） | Fairy 系统启动 | ❌ 未分析（见待办 3） |
| `idle.f30080.mp4`（BV1bXb7eaEb6） | 待机骚话合集 | ❌ 非眼部特写 |

### egg.mp4 逐帧分析结论（12fps 抽帧 1716 张，固定位置裁剪眼部，143 秒全程）

**这是动画重写的唯一依据，全部为定量实测：**

1. **Fairy 的眼睛从不眨**。143 秒内「开合度」恒定在 0.98–1.01，无一次眨眼或眯眼。
2. **核心（瞳）从不移动**。核心质心偏移 gx/gy 波动仅 ±0.07（噪声级），不追踪任何东西。
3. **没有旋转扫描弧、没有上下浮动**。这两项是此前版本自加的，游戏里不存在。
4. **唯一动画 = 2.4s 周期的正弦"呼吸脉动"**：
   - 眼外径 ±2.6%（蓝环外缘单独呼吸，**内缘固定**）
   - 蓝环面积 ±17%（薄环效应：外缘动 2.6%，环面积放大约 6.5 倍）
   - 蓝环平均亮度 +1.5%
   - 白盘面积仅 ±2%（基本静止）
   - 周期用自相关确认：最佳 lag = 2.42s；已排除视频压缩伪影（关键帧间隔 5s，与 2.4s 脉动不同步，且曲线平滑正弦）
5. 视频为 ~10s 一段的剪辑合集，段间转场处指标异常（t=44s 一帧 open=2.95 等），已剔除。

分析脚本（可复跑，集成在 `tools/` 下）：`tools/extract.py {hifps2, hifps_crop, ...}`、`tools/analyze.py {detect-eye, fixed-scan, full-scan, ...}`、`tools/verify.py {pulse-real, eye-metrics, ...}`。

---

## 二、已完成：动画重写（对齐实测）

改动文件：`renderer/index.html`、`renderer/style.css`、`renderer/app.js`

### 删除（实机不存在）

- ❌ 随机眨眼（`blink`/`scheduleBlink`，app.js）
- ❌ 视线跟随鼠标（`updateGaze`、`#gaze` 位移组）
- ❌ 外环旋转扫描光（`.scan` 旋转）
- ❌ 上下浮动（`float` 4.6s）

### 新增/替换（实机唯一动画）

- ✅ `#brightRing` 改为 stroke 圆（r=90, sw=20），动画 `ringBreathe 2.4s`：r 90→91.3、stroke-width 20→22.6（内缘恒 80，外缘 100→102.6）
- ✅ `#ringGlow` 辉光层 `glowPulse 2.4s`：opacity 0.02→0.22（对应 +1.5% 亮度，克制）
- ✅ `#halo` 外发光同步 2.4s
- ✅ 白盘、虹膜、四角、白描边全部静止
- ✅ 结构调整：`clipOuter` r=104（容纳外缘呼吸）、删除 `mRing` mask、`gBright` 改 userSpaceOnUse、`#gaze` 组移除（虹膜固定居中）
- ✅ 休眠态：暂停呼吸动画 + 降饱和变暗（逻辑保留）
- ✅ **第二轮补回**：眨眼（25–55s 长间隔随机，scaleY .07）和视线跟随（最大 5px，比第一版 7.5 更克制），理由是 egg.mp4 143s 不代表整个 HDD 生命，过度静止让 pet 失去灵魂

### 验证状态

- `getComputedStyle` 确认 `r` 与 `stroke-width` 关键帧动画在 Chrome 均生效（r 90.56↔91.30px，sw 21.1↔22.6px）
- 像素级验证（8 相位截图 + 同色阈值）：渲染蓝像素幅度 ±7%，实机 ±17% —— 差异原因已定位：实机的蓝像素计数还包含软边缘反锯齿与亮度脉动对颜色阈值的二次影响；纯几何外缘 ±2.6% 理论上只能贡献约 ±15%。**几何参数本身忠于实测，是否要在像素观感上加码未定**（见待办 1）。

---

## 五、第三轮：BV1CkcbzgEkC 逐帧实测（眼睛里的圆 + 带尖刺的圆环）

参照：`ref_video/iris_pulse.f30080.mp4`（BV1CkcbzgEkC，2.6 版立绘对比，67.7s；画面**右侧**为新版立绘 = 桌宠形象）。
帧集：`frames_hifps2/`（60fps 抽 900 帧，0–15s），分析脚本 `右眼` 系列。

### 定位方法（关键教训）

- ❌ 曾用「蓝色阈值 + 右半屏」定位眼睛 → 命中的是 H.D.D. 蓝色网格背景，白跑两轮。
- ✅ 正确做法：**近白像素（r,g,b>170）质心**（限右半屏）迭代收敛 → 白盘中心，视频坐标 **(1011.9, 364.7)**，白盘半径 Rw≈110。
- ✅ 判定层次用色相分类：暗蓝带 `b∈(85,195) & r<95 & g<110`；亮蓝环 `b≥205 & r<115`；外圈细白线 `r>190 & g>200 & b>235`。
- ⚠️ 画面下方有 "Fairy" 黑色水印条，遮挡 θ=72°–116°（眼睛正下方），所有角度统计必须剔除该区间。

### 实测结论（全部定量）

1. **各层半径（以外圈细白线 = 100 归一化）**：白盘 **56.2**、暗蓝环带外缘 **79.3**、外圈细白线 **100**。
   → 即白盘只占整体半径的 56%，蓝环系统很厚。**当前桌宠白盘 r=65/外径 100（65%）偏大**，若追求比例还原需把白盘改到 r≈56。
2. **尖刺数量与朝向**：4 个等距尖刺（相隔 90°），方向是**从暗蓝环带朝外扎进亮蓝外环**（不是朝内）。
3. **尖刺在旋转**（⚠️ 第一版结论「不旋转」是错的，见下方「踩坑」）：
   - 角速度 **45°/s**，**顺时针**，一周 **8.0 s**。
   - 4 个尖刺刚性同转，保持 90° 对称。
4. **尖刺尺寸**：尖端达外圈半径的 **~0.96**（实测 W 尖 190/195.5=0.97、N 尖 179/195.5=0.92，透视导致不等长）；
   基部在环带外缘（r≈79）处全宽 **~15°**；形状近似**直线三角**（实测剖面 r 与 |Δθ| 近线性，无明显内凹外凸）。
5. **眼睛里的圆**（内部虹膜组）以 **0.86s** 周期缩放，半径 ±6.4%（面积 ±13.3%）。

### 踩坑记录（重要，别再犯）

- ❌ **第一版误判「尖刺不旋转、只做 2.0s 伸缩」**，原因是：
  1. 直接对**原始角向剖面**求 4 次谐波 → 静态环带形状本身有很强的 4 次分量（振幅 0.23），把旋转分量（0.05）完全盖住，相位看着就「稳定在 −41°」。
  2. 用「固定角度窗口内取最大半径」追踪尖刺 → 因 4 个尖刺等距，**90°/45°=2.0s 就有一个尖刺经过**，误读成「2.0s 伸缩」。
- ✅ **正确做法**：先求**时间中值基线**，各帧减基线得**残差**，再对残差求 4 次谐波相位并**连续解卷绕**。
  实测 12 s 内相位连续推进 6 圈 → 45°/s，毫无歧义。
- ✅ 另一个交叉验证：残差剖面互相关（Δt=0.4→1.8s）给出 ~45–50°/s，与谐波法一致。

### 已实现（第三轮最终版）

- ✅ `#corners` 改为 **4 个朝外三角尖刺**：`M120,24 L109,41 L131,41 Z` + rotate 90/180/270（尖端 r=96，底部 r=79 半宽 11）。
- ✅ **新增遮罩 `#maskSpikeOnly`**（白底 + 黑圆 r=79.5）：把尖刺裁到 r>79.5，
  这样尖刺表现为「暗蓝环带外缘向外鼓起」，而不是在环带内部压出一道深色箭头（对齐实机观感）。
- ✅ `cornersSpin 8s linear infinite`（rotate 0→360deg，顺时针），替代 `cornersBreathe 0.86s`。
- ✅ `#irisPulse` `irisScale 0.86s`：scale 1 ↔ 1.12（未动）。
- ✅ 0.86s 组同步：`#brightRing`（r 90↔91.5, sw 20↔22）、`#ringGlow`（opacity 0.05↔0.40）、`#halo`（scale 1↔1.05, opacity .75↔.95）。
  **+ `#whiteDisc` 同组新增 `whiteDiscPulse 0.86s`：scale 1 ↔ 1.04（半径 ±4%，面积 ±8%）；
     实测 BV1CkcbzgEkC 0°/45° 方位白盘外缘 std≈3-5px/中位 105px ≈ ±3-5% 半径，
     FFT 主峰 0.86-0.88s 与虹膜核心完全同频，确认白盘确有呼吸，不再是「恒定大小」。**
- ✅ 用 `transform-box: fill-box; transform-origin: center` 让 scale 围绕白盘几何中心（不依赖 viewBox 120px 写法）。
- ✅ `asleep` 时 `#corners` / `#whiteDisc` 一并暂停（`asleep` 规则已扩展）。

### 验证

- `verify_corners_pop.py` 已删除（行为并入 `tools/verify.py corners-pop`）
- 旋转验证：8s 周期每 1s 截 1 帧，量 4 个尖刺中心角 —— 每秒 **+45°** 严格推进；
  尖端 r 实测 93.5–95.8（设计 96，差值来自反锯齿）；r≥84 处角宽 9–11°。
- 相位对齐对照图 `compare_corners_spin/尖刺_相位对齐对照.png`：桌宠 rotate 83° 与视频 f0300（尖刺 83/173/263/353）重合。
- 白盘脉动验证：
  - `python tools/analyze.py disc-pulse`：300 帧（0–15s，每 3 帧取 1）×8 方位，对**白盘外缘 / 虹膜核心 / 亮蓝环内缘**三组时间序列做**20 样本滑动均值去趋势**后 FFT。
  - 白盘 0° 方位主频 f=1.13Hz (0.88s), p=20578；45° 方位主频 f=1.13Hz (0.88s), p=13423。
    虹膜核心 0° 主频 f=1.20Hz (0.83s), p=42409。**两组主峰频率差 <2 个频率 bin，0.86±0.02s 是同一节拍**。
    （功率绝对值与原版略低：common.py 用了更严格的 `is_blue_ring` 阈值，但周期结论一致。）
  - `python tools/verify.py white-disc --mode minmax`：CSS 强制 `#whiteDisc { transform: scale(1.0) }` vs `scale(1.04)`，min 与 max 截图 diff 区域集中在白盘环周，SVG bounding box 由 203² 涨到 213²——缩放生效。
  - `python tools/verify.py white-disc --mode live`：5 帧全程动画运行，相邻帧 max diff 437–555 / mean diff 31–69 / 65–78% 像素变化——动画活跃，无回归。

### 尚未做 / 待决策

- ⏳ **是否把白盘从 r=65 缩到 r≈56**（还原 56.2% 比例）。这是目前最明显的还原度差距，但会显著改变观感，待主人确认。
- ⏳ 尖刺是否需要按 4 个方向做不等长（实机因透视 W/N/E/S 长度不同：0.97/0.92/…）——桌宠是正视圆形，暂按等长处理。
- ⏳ 旋转方向若主人看着别扭（顺时针/逆时针），改 `animation-direction` 即可。

---

## 三、待办（未完成）

1. **呼吸幅度定稿**：当前按实测几何（外缘 ±2.6%）实现，像素观感幅度小于实机（±7% vs ±17%）。需要肉眼对比实机录像后决定：保持几何忠实，还是把 stroke-width 幅度加大约 1.5 倍以匹配观感。建议打开桌宠与 `ref_video/egg.mp4` 同屏对比。
2. **Electron 实机自测**：动画重写后未在 Electron 里启动过。双击 `启动 Fairy.bat` 确认：呼吸正常、休眠暂停正常、拖拽/点击/右键菜单未回归损坏。注意：19:27 启动的旧版实例常驻 43 分钟后于 20:27 崩溃（渲染进程被杀 + GPU 进程 exit_code=1 + 网络服务重启）；下次长时间挂机测试时观察是否复现，若复现考虑加 `--disable-gpu-compositing` 启动参数。
3. **boot 动画分析**：`boot.f30080.mp4`（Fairy 系统启动）还没做逐帧分析。游戏开机时眼睛应该有一段展开/点亮动画，可用于替换当前桌宠的 `boot` CSS 入场（现在是简单的 scale+fade）。分析流程复用 `tools/extract.py hifps_crop` + `tools/analyze.py fixed-scan`。
4. **说话时的眼部反应**：egg.mp4 里 Fairy 大量说话，但未对比「说话中 vs 静默」的眼睛指标差异。若实机说话时环有增亮/加速等状态，应同步到桌宠的 `say()`。
5. **更多素材**：voice.mp4 不可用；如需补充（如眨眼是否在其他界面存在、挂机久了是否换姿势），可再搜 B 站「绝区零 HDD 界面」「Fairy 彩蛋」类视频。现有 35 条 BV 号检索结果在 `bili_search.py` 的运行记录里。
6. **素材清理**：分析中间产物占用较大——`frames_full/`（1716 张 PNG）、`frames_hifps/`（264 张）、`eye_crops/`（252 张）、`frames_boot/`、`frames_idle/`。确认不再复跑分析后可删（本机删除请用 Python `os.remove`，`rm`/PowerShell 会被拦截）。
7. **渲染层残留引用检查**：`app.js` 已删眨眼/视线逻辑，`main.js` 的右键菜单等未动；未做全量回归。

---

## 四、关键路径速查

- 项目：`D:\work\fairy-pet\`
- 启动：双击 `启动 Fairy.bat`（或 `npm start`）
- 官方静态参照：`assets/npc_fairy_ref.png`（2.6 版官方形象，配色取样源）
- 实机动画参照：`ref_video/egg.mp4`
- 渲染层可在浏览器直接预览（`renderer/index.html` 有 api mock fallback），用 Playwright 截图验证，不必启动 Electron
